#include <stdio.h>

int main(void)
{
    int m, n;
    scanf("%d %d", &m, &n);
    printf("Your input m is %d, n is %d\n", m, n);

    char ch;
    scanf(" %c", &ch);
    printf("Your input ch is %c\n", ch);
    return 0;
}
